# this repo may appear when updating our copy of s223
223standard

# cache of ontologies
.ontoenv

# compiled ontology documents (unversioned "latest" and versioned snapshots)
build/

# ignore uv.lock
uv.lock

# Having xcode issues with new computer, need to fix before using uv
# - using venv while figuring out
venv
__pycache__/
.DS_Store
# ignore logs?
*.log
docs/_build
.ipynb_checkpoints